import styled from "styled-components";

export const List = styled("ul")(() => {
    return {
        fontSize: "24px",
        color: "wheat",
        textAlign: "left",
        margin: "auto",
        width: "260px",
    }
})