import styled from "styled-components";

export const NotificationMessage = styled('p')(() => {
    return {
        // color: "#4f4fc2",
        color:"white",
        fontSize: "36px",
        textAlign: "center",
        fontWeight:"500"
    }
})